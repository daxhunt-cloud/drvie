---
id: T-003
slug: routebook-prd-and-improvement-audit
version: v1
title: "docs: CLAUDE.md 애니메이션 길이 30초 → 45초 정정"
status: done
stage: phase3_ticket_spec
assignee: pdt-developer
source_audit_finding: C-2
estimated_complexity: L1
risk_flags: [docs-only]
priority: P0
started_at: "2026-05-22T04:42:13Z"
completed_at: "2026-05-22T04:45:19Z"
duration_min: 2
routing: { model: haiku, effort: low }
created_at: "2026-05-22T04:25:18Z"
---

# T-003 · docs: CLAUDE.md 애니메이션 길이 30초 → 45초 정정

> status: done · stage: phase3_complete · assignee: pdt-developer
> (mirrored header — PO updates mechanically on lifecycle change)

## Request

`CLAUDE.md`가 3개 위치에서 애니메이션 길이를 "30초"로 기재하고 있으나 실제 코드는 `FIXED_DURATION_SEC = 45`(`src/components/CoursePlayer.tsx:10`)이며 음악 구간 계산도 `musicEndSec = musicStartSec + 45`(`src/components/Map.tsx:565`)로 동작한다. 코드가 truth이므로 CLAUDE.md를 45초로 통일한다. 감사 항목 **C-2** 에서 식별된 문서 버그다. 단, `src/components/Map.tsx:2307`의 UI 레이블("시작 지점 (45초 자동 재생)")은 **이미 올바르므로 변경 불필요**하다.

## Approach

**수정 대상: `CLAUDE.md` 3개 위치**

| 위치 | 현재 | 변경 후 |
|---|---|---|
| `CLAUDE.md:5` | `30초 3D 애니메이션으로 미리보기하고` | `45초 3D 애니메이션으로 미리보기하고` |
| `CLAUDE.md:50` | `# 30초 3D 애니메이션 + 음악` | `# 45초 3D 애니메이션 + 음악` |
| `CLAUDE.md:117` | `- 영상 길이 고정 30초 (\`FIXED_DURATION_SEC\`)` | `- 영상 길이 고정 45초 (\`FIXED_DURATION_SEC\`)` |

**변경 없는 파일**

- `src/components/Map.tsx:2307` — `"시작 지점 (45초 자동 재생)"` 이미 정확. 수정 불필요.
- `src/components/CoursePlayer.tsx:10` — `const FIXED_DURATION_SEC = 45;` 코드 truth. 수정 불필요.

**검증 방법**: 수정 후 `grep -n "30초" CLAUDE.md` 로 잔존 건수 확인.

> **Note**: "30초"라는 표현이 CLAUDE.md 외 다른 문서/코드에 남아 있는지 전체 grep 후 확인 필요. 이 티켓의 개선 감사 보고서(`docs/prd/routebook-improvement-audit.md`)와 `docs/prd/routebook-as-built.md` 내 "30초" 언급은 **히스토리 기록으로서 보존** (수정 불필요).

## Acceptance

- [ ] `grep "30초" CLAUDE.md` 결과 **0건** (3개 위치 모두 수정 완료)
- [ ] `src/components/Map.tsx:2307` 에 `"45초 자동 재생"` 레이블이 그대로 유지되는 것을 확인 (변경 없음 검증)
- [ ] `src/components/CoursePlayer.tsx:10` 의 `FIXED_DURATION_SEC = 45` 값이 그대로인 것을 확인 (코드 변경 없음 검증)
- [ ] `grep -rn "30초" src/` 결과 0건 (소스코드 내 30초 잔존 없음)
- [ ] CLAUDE.md 수정 후 전체 문서 맥락이 자연스러운지 검토 (30→45 교체로 인한 문장 어색함 없음 확인)

## Out of scope

- `src/components/CoursePlayer.tsx`의 `FIXED_DURATION_SEC` 값 변경 (코드 변경 없음)
- UI 텍스트 변경 (Map.tsx Step 4 이미 정확)
- `docs/prd/` 감사 보고서 내 인용 "30초" 수정 (히스토리 기록으로 보존)
- 마케팅/랜딩 페이지의 "30초" 문구 확인 — 랜딩(`src/app/page.tsx`) 별도 확인 권장 (본 티켓 범위 밖)

## Notes / risks

- **위험도 없음**: CLAUDE.md는 개발자 참조 문서이며 빌드·런타임에 영향 없음.
- **추가 확인**: `src/app/page.tsx`(랜딩 페이지)에 "30초" 마케팅 문구가 있을 수 있음 — 해당 파일 별도 grep 후 필요 시 현황에 맞게 수정. 의도된 마케팅 언어라면 유지.
- **롤백**: Git revert 1커밋.

## Persona Activity
<!-- PO appends 1-row per delegation, mechanical -->
| 2026-05-22 04:25Z | pdt-designer | sonnet/high | spec | ticket spec drafted (CLAUDE.md 3 lines; UI label already correct) |
| 2026-05-22 04:45Z | pdt-developer | haiku/low | impl | CLAUDE.md 3 lines 30초→45초; grep verification clean |
